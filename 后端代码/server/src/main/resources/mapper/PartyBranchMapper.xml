<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.server.mapper.PartyBranchMapper">

    <resultMap id="BaseResultMap" type="com.example.pojo.entity.PartyBranch">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="leader_id" property="leaderId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, leader_id, created_at
    </sql>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM party_branches
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM party_branches
        ORDER BY id ASC
    </select>

    <!-- 更新支部：名称、负责人、可扩展负责人电话（借用admins表mobile） -->
    <update id="update" parameterType="com.example.pojo.entity.PartyBranch">
        UPDATE party_branches
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="leaderId != null">leader_id = #{leaderId},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>


